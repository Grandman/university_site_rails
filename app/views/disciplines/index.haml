%h1 Список предметов
%table
  %thead
    %tr
      %th Название
      %th Преподаватель
      %th{:colspan => "3"}
  %tbody
    - @disciplines.each do |discipline|
      %tr
        %td= discipline.name
        %td= discipline.user.try(:name)
        %td= link_to 'К странице', discipline_path(discipline)
        %td= link_to 'Редактировать', edit_discipline_path(discipline)
        %td= link_to 'Удалить', discipline, method: :delete, data: { confirm: 'Вы уверены?' }
%br/
= link_to 'Создать предмет', new_discipline_path
